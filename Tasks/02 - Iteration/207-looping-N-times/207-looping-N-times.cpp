#include <stdio.h>

int main()
{
    //Counter variable
    


    //For loop (pre-C99 standard)
    for (int i = -10; i <= 20; i += 5)
    {
        //Perform code tasks
        printf("i=%d\n", i);
    }

    // TASK A - count from 10 down to 0 in steps of -1 ,done

    // TASK B - count from 10 down to -10 in steps of -2 ,done

    // TASK C - count from 10 to 200 in steps of 10 ,done

    // TASK D - count from -10 down to 20 in steps of 5 , done

    // TASK E - can you predict the largest value of i that is displayed?, 9, correct.
    for (int i = 0; i < 12; i += 3)
    {
        //Perform code tasks
        printf("i=%d\n", i);
    }
}